name: clean_way
description: "CleanWay FrontEnd flutter project"
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: '>=3.3.4 <4.0.0'

dependencies:
  intl: ^0.19.0
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.6
  image_picker: ^1.1.1
  http: ^1.2.1
  flutter_dotenv: ^5.1.0
  flutter_naver_map: ^1.2.2
  geolocator: ^11.0.0
  change_app_package_name: ^1.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^3.0.0

flutter:

  uses-material-design: true

